// Recursive factorial -- exercises function definition, conditionals,
// and recursion (all forward-declarable in Selenium).

ritual fact(int n) -> int {
    eclipse (n <= 1) {
        return 1;
    } shadow {
        return n * fact(n - 1);
    };
};

whisper fact(5);
